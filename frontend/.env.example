# URL del Worker deployado en Cloudflare
# En desarrollo local, dejar vacío (Vite proxea a localhost:8787)
# En producción, poner la URL del Worker:
VITE_API_URL=https://guarani-unificado-api.ffrusal.workers.dev
